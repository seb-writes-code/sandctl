# sandctl (TypeScript + Bun)

This directory contains the TypeScript rewrite of `sandctl`.

## Prerequisites

- Bun 1.x

## Commands

```bash
cd sandctl-ts
bun install
bun run lint
bun test
bun run build
./sandctl --help
./sandctl version
```
